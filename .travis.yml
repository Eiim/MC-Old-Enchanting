language: node_js
node_js:
  - node
deploy:
  provider: pages
  skip_cleanup: true
  local-dir: dist
  github_token: $GITHUB_TOKEN
  keep_history: true
  target-branch: gh-pages
  verbose: true
  on:
    branches:
      only:
        - main
script: 
  - npm run build
